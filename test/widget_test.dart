import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // TODO: Add meaningful widget test when widget tree is testable
    expect(true, isTrue);
  });
}
